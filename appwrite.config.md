- run

  `appwrite pull settings tables buckets teams topics` at root because we don't need to pull functions for now

- sometimes we need to manually remove info in appwrite.config.json after an update in appwrite cloud console, then re-run the pull command

- COPY BELOW:

appwrite pull settings tables buckets teams topics

appwrite pull settings
